{
  "name": "uxistentialism-studio-companion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Studio Sync companion daemon (WS-2): watches the Obsidian vault and the Editorial Board inbox, projects, validates, and submits authenticated payloads to the hosted Studio.",
  "scripts": {
    "start": "node index.mjs",
    "test": "node tests/run.mjs"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "picomatch": "^2.3.1"
  }
}
